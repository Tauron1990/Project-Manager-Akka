@inherits Tauron.Grid.Internal.DefinitionBase

@code {
    [Parameter]
    public Lenght Height { get; set; } = Lenght.FromFraction(1);

    protected internal override string PropertyName => "grid-template-rows";
    protected override Lenght Value => Height;
}