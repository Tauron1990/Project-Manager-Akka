@inherits Tauron.Grid.Internal.DefinitionBase

@code {
    [Parameter]
    public Lenght Width { get; set; } = Lenght.FromFraction(1);

    protected internal override string PropertyName => "grid-template-columns";
    protected override Lenght Value => Width;
}