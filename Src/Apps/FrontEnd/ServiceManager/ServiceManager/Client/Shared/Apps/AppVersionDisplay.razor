@using Tauron.Application.Master.Commands.Deployment.Build.Data
@if (Item.LastVersion == -1)
{
    <MudText>Keine Version Verfügbar</MudText>
}
else
{
    <MudText>@Item.LastVersion</MudText>
}

@code {

    [Parameter]
    public AppInfo Item { get; set; } = AppInfo.Empty;

}