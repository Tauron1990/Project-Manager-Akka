@page "/"
@using ServiceManager.Shared.Identity

@* ReSharper disable once Html.TagShouldNotBeSelfClosed *@
<Title Value="Service Manager"/>

<GenericAuthorizeView Claims="new[] { Claims.ClusterNodeClaim }">
    <IndexView/>
</GenericAuthorizeView>

@code
{
    protected override void OnInitialized()
    {
        Console.WriteLine("Index Initialized");
        base.OnInitialized();
    }
}