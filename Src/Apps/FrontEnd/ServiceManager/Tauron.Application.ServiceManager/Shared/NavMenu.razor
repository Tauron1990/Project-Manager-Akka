
@inherits ConnectionAwareComponent

<MudDivider Class="mb-4"></MudDivider>
<MudNavMenu>
    <MudNavLink Href="" Match="NavLinkMatch.All">Übersicht</MudNavLink>
    <MudBadge Icon="@_configError">
        <MudNavLink Href="/AppConfiguration"></MudNavLink>
    </MudBadge>
</MudNavMenu>

@code {

    private string? _configError;
    
    private bool _collapseNavMenu = true;

    private string? NavMenuCssClass => _collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        _collapseNavMenu = !_collapseNavMenu;
    }

    protected override void OnParametersSet() 
        => _configError = IsDatabaseReady ? null : Icons.Material.Filled.Warning;

}
